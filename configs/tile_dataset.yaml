# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - _self_
  - local: tiling.yaml
  - paths: localized_tiling.yaml

# tile_size: 373 # 85um
tile_size: 224 # 51um
tile_annotations: true
prefix: reinhard
out_dir: tiles_smaller
normalization: reinhard